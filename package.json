{
  "name": "API project",
  "version": "1.0.0",
  "description": "",
  "main": "script.js",
  "scripts": {
    "start": "parcel src/index.html --public-url ./",
    "build": "parcel build src/index.html",
    "deploy": "gh-pages -d dist",
    "cleanup": "rm -rf .cache dist",
    "dev": "npm run cleanup && parcel src/index.html"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.12.5"
  },
  "homepage": "https://faralex.github.io/Covid Statistics API",
  "dependencies": {
    "dotenv": "^16.0.2",
    "gh-pages": "^4.0.0"
  }
}
